import { weekend } from '../../../helpers/weekend';
import { module, test } from 'qunit';

module('Unit | Helper | weekend');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = weekend(42);
  assert.ok(result);
});
